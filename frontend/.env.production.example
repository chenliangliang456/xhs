# Vercel 部署：前后端同域时可留空
# 若 API 部署在其它域名，填后端根地址（无末尾斜杠），例如 https://api.example.com
VITE_API_ORIGIN=

# 多服务前缀（你当前 vercel.json backend.routePrefix="/_/backend"）
# API 默认是 /api；多服务时请改成 /_/backend/api
VITE_API_PREFIX=/_/backend/api

# 静态资源前缀（uploads/materials）
VITE_ASSET_PREFIX=/_/backend
